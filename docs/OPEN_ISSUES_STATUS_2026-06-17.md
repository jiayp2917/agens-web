# 遗留问题处理状态

本文只记录当前遗留项的处理状态，供后续智能体接续使用。

## 已处理到代码层

- USB 调试链路已恢复到基础可用：`adb devices -l` 能看到授权设备，已能执行 `screencap` 并拉取到 `D:\chat\plan\agens_adb_probe.png`。
- 引导模式仍是唯一可用模式；角色创建页展示小说模式、游戏模式，但二者置灰不可点击。
- 无 key、模型失败或模型无有效选项时，用户选择“本地兜底继续”会进入本地预设故事，不再只给固定兜底选项。
- 本地故事状态已进入存档结构：记录 `local_story.active/story_id/node_id`，读档后可恢复当前节点和 A/B/C。
- 模型异常已拆分为请求失败、输出不完整、审判失败、本地兜底等类别；UI 弹窗会显示更具体的原因，logcat 记录脱敏诊断信息。
- 叙事声明获得物品、功法、地点、任务或境界变化但缺少结构化 delta 时，引擎拒绝直接改状态，并提示以状态栏为准。
- D 输入栏已有当前输入预览，发送后会在叙事区回显玩家输入。

## 仍需真机验证

- 安装最新 APK 后验证：三模式卡展示、小说/游戏不可点、本地故事 A/B/C 和 D 输入都能推进。
- 验证本地故事存档：进入本地故事后手动存档、退出、读档，确认节点和选项一致。
- 验证模型异常弹窗：无 key、输出不完整、Judge 失败至少覆盖一种，确认 UI 文案和 logcat 原因一致。
- 验证 D 输入软键盘：聚焦前、键盘弹出、中文输入、发送后截图各一张。
- 验证“更多”面板：背包、功法、地图、任务随结构化 delta 变化。

## 暂不处理

- 不开放小说模式和游戏模式，只保留禁用入口。
- 不实现完整 60-120 回合离线游戏模式；当前只做一套本地故事兜底。
- 不恢复 CLI/REPL、三档自由度、常驻战斗按钮或 Windows 桌面点击验证。
- 不在本轮大拆 `GameEngine`，只记录后续架构风险。
