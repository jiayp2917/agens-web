# Mobile dependencies for Android APK builds.
# The game engine uses in-repo agent runners and httpx to call a remote LLM API.
# No local model inference or LangGraph/LangChain runtime is needed on Android.

# Core UI
kivy==2.3.0

# HTTP client for LLM API calls
httpx>=0.27.0
